server:
  port: 8080
  
spring:
  application:
    name: CRZuul

# zuul连接超时配置      
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000
ribbon:
  ConnectTimeout: 3000
  ReadTimeout: 60000
       